# Точка продолжения работы — читать в начале новой сессии

## Актуализация 2026-07-24 — локальная проверка перед Этапом 3

- Живой сервер `e95ca529-f434-4432-bbb8-a7e0e8f85837` проверен повторно. После `POST /v1/infra/servers/:id/wake?wait=true` сервер вернул `RUNNING`/`CONNECTED`; `/exec` подтвердил `systemctl is-active app -> active`, наличие `VIBE_APP_KEY` и `VIBE_LAYOUT_FOLDER_ID=90` в `/opt/app/.env` без раскрытия секрета, `curl http://localhost:3000/api/health -> {"status":"ok"}`. Внешний `npm run vibecode:check-server` с личным ключом подтвердил `/api/health` (200) и React SPA (200, `text/html`).
- UF-поля сделок на актуальном `vibecode01` созданы и проверены 2026-07-24. Подтверждены 11 кодов: `UF_CRM_PRODUCT_TYPE`, `UF_CRM_FABRIC`, `UF_CRM_CARE`, `UF_CRM_PRINT_METHOD`, `UF_CRM_BODY_COLOR`, `UF_CRM_TRIM_COLOR`, `UF_CRM_SIZES_JSON`, `UF_CRM_OPTIONS`, `UF_CRM_PRINT_ZONE`, `UF_CRM_COMMENT`, `UF_CRM_DESIGN_FILE_ID`; поле `UF_CRM_DESIGN_FILE_ID` имеет тип `file`. Повторный `npm run vibecode:ensure-fields` с `VIBE_PERSONAL_API_KEY` идемпотентно вывел `UF exists` для всех 11 полей.
- `CRM_DEAL_DETAIL_TAB` заново привязан к актуальному app id `2f3c0ba9-4253-4da4-9aaa-6a9ee4bb4e61` через app-key, сохранённый только на сервере в `/opt/app/.env`: `POST /v1/placements/bind` вернул `success=true`, `handlerRewritten=true`, `alreadyBound=true` при повторе; `GET /v1/placements` вернул `placements=["CRM_DEAL_DETAIL_TAB"]` и тот же app id.
- `scripts/vibecode-ops.mjs` обновлён под реальный API: `ensure-fields` умеет использовать `VIBE_PERSONAL_API_KEY`, `bind-placement` требует OAuth `VIBE_APP_KEY`, а `placements/available` разбирается из группового формата `data.groups`.
- Локальный контроль после правок: `npm test` проходит (backend 26, frontend 22, shared-types 2), `npm run build` проходит, `npm run lint` проходит.
- Локальные проверки выполнены вне sandbox Windows, потому что внутри sandbox Vitest/Vite падают на `spawn EPERM` при запуске дочерних node-процессов. `npm test` проходит: backend 26 тестов, frontend 22 теста, shared-types 2 теста. `npm run build` проходит; остаётся только штатное предупреждение Vite о крупном JS chunk.
- В `packages/shared-types` исключены `src/**/*.test.ts` из `tsc`-сборки, а test-скрипт ограничен `vitest run src`, чтобы после build пакет не выпускал `dist/index.test.js` и тесты не прогоняли скомпилированные копии.
- В `apps/frontend/src/features/order/exportOrderPdf.test.ts` замокан `HTMLAnchorElement.prototype.click`, чтобы jsdom не шумел ошибкой `Not implemented: navigation` при тестировании автоскачивания PDF.
- Важное наблюдение: текущий локальный `.env` содержит `VIBE_APP_KEY`, который `GET /v1/me` определяет как `type="oauth_app"`, `portal="vibecode02.bitrix24.ru"`, scopes `crm,disk,placement`. Это старый портал. Этим ключом нельзя выполнять будущие операции с актуальным `vibecode01`; актуальный app-key нового app id `2f3c0ba9-4253-4da4-9aaa-6a9ee4bb4e61` сейчас есть в env живого сервера, сырой секрет в репозиторий не записан.
- Добавлен ops-скрипт `scripts/vibecode-ops.mjs` и npm-команды: `npm run vibecode:check-server`, `npm run vibecode:ensure-fields`, `npm run vibecode:bind-placement`, `npm run vibecode:setup-portal`. Скрипт сначала проверяет `/v1/me` и отказывается менять портал, если ключ не относится к `VIBE_EXPECTED_PORTAL` (по умолчанию `vibecode01.bitrix24.ru`). `ensure-fields` может работать личным ключом, `bind-placement` требует актуальный OAuth app-key.
- `npm run vibecode:check-server` без `VIBE_PERSONAL_API_KEY` проверяет публичный owner-only probe и сейчас получает ожидаемый `401 BH_LOGIN_REQUIRED`, то есть домен/BlackHole gateway живой и защищён. Для полной проверки `/api/health` и SPA через bearer-token нужен личный `VIBE_PERSONAL_API_KEY` актуального `vibecode01`.

## Актуализация 2026-07-23 — новый тестовый портал

- Актуальный тестовый портал теперь **`vibecode01.bitrix24.ru`**. Все упоминания `vibecode02.bitrix24.ru`, старого `vibe_app_...`, server id `801496ff-f700-4dd0-9bec-571adbc7efcb` и appUrl `https://app-2a2f90ace69d.vibecode.bitrix24.tech` ниже считать историческим контекстом предыдущего портала, а не рабочей инфраструктурой.
- Новый личный ключ VibeCode для `vibecode01` передан пользователем в чате и проверен через `GET https://vibecode.bitrix24.tech/v1/me`: `success=true`, `type=personal`, `portal=vibecode01.bitrix24.ru`, `accessMode=READWRITE`, scopes включают `crm`, `disk`, `placement`, `vibe:infra`, `vibe:storage`.
- Секреты не записывать в документацию, код или git. Для локальной работы ключ должен быть только в `.env`/секретах окружения как `VIBE_APP_KEY` или использоваться одноразово в командной строке при живых проверках.
- Первичная проверка нового контура 2026-07-23: `GET /v1/infra/servers` и `GET /v1/apps` сначала вернули пустые списки; затем пользователь вручную создал рабочий BlackHole-сервер, а приложение/ключ/папка Диска были подняты через API.
- Проверка `GET /v1/me` → `data.capabilities` 2026-07-23: `servers.create.available=false`, `servers.deploy.available=false`, причина `MARKETPLACE_REQUIRED`; сообщение платформы: серверы доступны на коммерческих тарифах Битрикс24 или во время активного триала VibeCode, ссылка активации `https://vibecode01.bitrix24.ru/settings/license/`. Это блокирует создание BlackHole/galaxyApp-сервера на новом портале до включения тарифа/триала, даже если сам `vibe_api_` ключ живой и `READWRITE`.
- После сообщения пользователя о включении 15-дневного доступа фактический `POST /v1/infra/servers` прошёл, но API создал `GALAXY_APP` на общем galaxy-хосте, где было поле `monthlyCost="2000"` у хоста. Пользователь попросил удалить этот контур; `DELETE /v1/infra/servers/:id` успешно удалил и приложение, и пустой общий galaxy-хост. После этого пользователь вручную создал актуальный STANDALONE/BlackHole сервер `e95ca529-f434-4432-bbb8-a7e0e8f85837`.
- Перед следующим созданием сервера обязательно проверять биллинг/админку и не продолжать деплой, если API показывает неожиданный `monthlyCost` у galaxy-хоста.
- Пользователь вручную создал правильный сервер через админку BlackHole. Актуальный сервер `vibecode01`:
  - id `e95ca529-f434-4432-bbb8-a7e0e8f85837`, kind `STANDALONE`, mode `BLACKHOLE`, plan `bc-micro`, monthlyCost `"600"`, region `ru-central1-b`, status `running`, blackholeStatus `CONNECTED`, accessPolicy `OWNER_ONLY`;
  - appUrl `https://app-5670766a17c1.vibecode.bitrix24.tech`, localPort `3000`, sleepAfterMinutes `30`;
  - metadata обновлена отдельным `PATCH /v1/infra/servers/:id`: `displayName="Garment Print Constructor"`, `description="MVP order constructor for garment print deals in Bitrix24"`.
- Деплой на этот сервер выполнен 2026-07-23 через `POST /v1/infra/servers/e95ca529-f434-4432-bbb8-a7e0e8f85837/deploy`: успешные steps `runtime`, `install`, `env`, `systemd`, `start`, `healthcheck`, `tunnel_routing`; `serviceName="app"`, `status="running"`.
- Выпущен нормальный OAuth/VibeCode app key через `POST /v1/apps`: app id `2f3c0ba9-4253-4da4-9aaa-6a9ee4bb4e61`, scopes `crm`, `disk`, `placement`, `user`, appUrl `https://app-5670766a17c1.vibecode.bitrix24.tech`. Сырой `vibe_app_...` ключ был возвращён один раз и не записан в документы.
- В "Общем диске" создана/переиспользована папка "Макеты конструктора": актуальный `VIBE_LAYOUT_FOLDER_ID=90`.
- Через `POST /v1/infra/servers/e95ca529-f434-4432-bbb8-a7e0e8f85837/exec` установлены системные библиотеки для Puppeteer/headless Chromium (`apt-get install ...` на Ubuntu 24.04, exitCode 0). Для долгих установок использовать `/exec` с `timeout=600`, а не `preStart`: у шагов deploy свой лимит 300 секунд.
- Сервер передеплоен с `PORT=3000`, `VIBE_APP_KEY=<актуальный vibe_app_...>`, `VIBE_LAYOUT_FOLDER_ID=90`. Проверено через `/exec`: `systemctl is-active app` -> `active`, `/opt/app/.env` содержит `VIBE_APP_KEY` и `VIBE_LAYOUT_FOLDER_ID="90"` без раскрытия ключа, `curl http://localhost:3000/api/health` -> `{"status":"ok"}`. Внешний `GET https://app-5670766a17c1.vibecode.bitrix24.tech/api/health` через временный access-token тоже вернул `200 {"status":"ok"}`.
- Практика упаковки: не использовать Windows `Compress-Archive` с полной копией workspace — VibeCode распакует, но появляются warnings про backslashes и лишние кэши. Рабочий вариант: чистый `tar.gz` из staging-директории с root `package.json`/`package-lock.json`/`tsconfig.base.json`, `packages/shared-types/{package.json,tsconfig.json,src/}`, `apps/backend/{package.json,tsconfig.json,src/}`, `apps/frontend/dist/`. Обязательно проверить `tar -tf`, что внутри есть `packages/shared-types/src/index.ts` и `apps/backend/src/index.ts`.
- Если Marketplace/VibeCode-подписки нет и приходится выбирать платный тариф для серверов вручную, выбирать **`bc-micro`** как минимальный практичный вариант для этого проекта: он дешевле полноценных VM-тарифов, но имеет 4 GB RAM. Старого бесплатного shared-контура примерно на 500 MB RAM больше не считать гарантированно доступным на новом портале.
- Проверка CRM UF-полей на новом портале 2026-07-23: `GET /v1/userfields/deals` вернул `403 BITRIX_ACCESS_DENIED` с сообщением `REST is available only by subscription.` До решения подписки/доступа REST нельзя подтвердить или создать UF-поля сделок на `vibecode01`.
- GitHub remote проверен: `origin=https://github.com/DjjoniiL/Project-AI-GIT.git`, локальная `main` совпадает с `origin/main` на `5f773e0`. `git push --dry-run origin main` проходит, значит текущее окружение имеет рабочий доступ на push без фактической отправки.
- Backend-тесты нужно прогонять локально из workspace `apps/backend`; они не требуют живого VibeCode-сервера, потому что внешние вызовы мокируются. Проверка ПО на новом VibeCode-сервере выполнена через `/exec`, системные библиотеки для Puppeteer установлены.

Этот файл — сводка для быстрого восстановления контекста в новой сессии Claude Code. Источник истины по архитектуре/требованиям — `specification.md`, второй обязательный документ — `README.txt`. Этот файл даёт быстрый снимок "что сделано / что дальше" без необходимости перечитывать историю чата.

## Что за проект

MVP веб-конструктора заказов на печать одежды (футболки/худи/свитшоты/лонгсливы), встраиваемого вкладкой в карточку сделки Битрикс24 через платформу **VibeCode** (не классический REST+OAuth2, см. `specification.md` раздел 2). Фронтенд собирает параметры заказа + макет принта, бэкенд создаёт/обновляет сделку и грузит файл на Диск Битрикс24.

## Текущий статус

- ✅ **Дизайн — завершён.** Финальный макет — [`design/maket-6.html`](design/maket-6.html), полностью согласован пользователем. Макеты 1–5 — история/точки отката, не удалять (отличия каждой версии — в шапке-комментарии файла).
- ✅ **Живой API VibeCode проверен и используется на новом портале** `vibecode01.bitrix24.ru`. Старый `vibecode02.bitrix24.ru` ниже оставлен только как исторический контекст.
- ✅ **Сетевая проблема с доступом к `vibecode.bitrix24.tech` — решена и понята.** Причина была НЕ в коде/ключах: у пользователя на машине активен Outline VPN, и его точка выхода (сервер) периодически не пропускала трафик до этого хоста (TCP иногда проходил, иногда нет; один раз дошло до TLS ClientHello и зависло — похоже на нестабильность/фильтрацию именно на этом VPN-сервере, не блокировку по домену в принципе, т.к. с другого сервера VPN всё сразу заработало). **Решение: переключить точку выхода Outline VPN на другую страну (сработало — Италия).** Если снова не будет коннекта — первым делом попробовать переключить/переподключить VPN, а не тратить время на диагностику кода. Диагностика: `curl` и `Test-NetConnection`/PowerShell могут вести себя по-разному (Bash-песочница агента вообще не видит VPN-интерфейс `outline-tap0`, поэтому для реальной проверки сети из этого проекта надёжнее гонять `curl.exe` именно через PowerShell-инструмент, не через Bash).
- ✅ **UF-поля созданы и проверены на новом портале `vibecode01`** (11 штук, `POST /v1/userfields/deals`) — коды совпали с `packages/shared-types/src/index.ts`. Важное открытие остаётся актуальным: Bitrix24 создаёт код поля как `UF_CRM_` + ровно тот `fieldName`, что передан в запросе.
- ✅ **Актуальный ключ `vibe_app_...` создан для `vibecode01`** (`POST /v1/apps`, scopes `crm`+`disk`+`placement`+`user`, app id `2f3c0ba9-4253-4da4-9aaa-6a9ee4bb4e61`) и передан в env живого сервера через `/deploy`. Сырой ключ в git/документацию не сохранялся.
- ✅ **Актуальная папка на Диске под макеты создана/переиспользована** — id `90` ("Макеты конструктора" в "Общем диске", storageId 3), передана на сервер как `VIBE_LAYOUT_FOLDER_ID=90`.
- ✅ **Важный фикс архитектуры бэкенда**: `packages/shared-types` раньше не собирался (`main`/`types` указывали прямо на `src/index.ts`) — локально это незаметно работало на Node 24 (нативная поддержка TS), но **сломало бы деплой**, так как VibeCode даёт только `node20` рантайм (без нативного TS). Добавлена сборка (`tsc`, `main`/`types` → `dist/`), порядок в корневом `npm run build` исправлен (shared-types собирается первым). Проверено: `node apps/backend/dist/index.js` реально стартует на чистой сборке.
- ✅ **Бэкенд и фронтенд задеплоены и работают на живой инфраструктуре VibeCode `vibecode01`.** Сервер `STANDALONE`/BlackHole, id `e95ca529-f434-4432-bbb8-a7e0e8f85837`, appUrl `https://app-5670766a17c1.vibecode.bitrix24.tech`, статус `running`/`CONNECTED`. `GET /api/health` подтверждён локально на сервере через `/exec` и снаружи через временный access-token (`{"status":"ok"}`), `accessPolicy` оставлен `OWNER_ONLY` и не менять без явного запроса пользователя.
- ✅ **Puppeteer/headless Chromium подготовлен на актуальном STANDALONE-сервере.** Системные библиотеки установлены разовым `POST /v1/infra/servers/:id/exec` с `timeout=600` (`apt-get install ...`, exitCode 0). На этом сервере долгие apt-операции лучше выносить в `/exec`, потому что у `preStart` в Deploy API лимит шага 300 секунд.
- ✅ **Placement для нового `vibecode01` заново привязан.** `CRM_DEAL_DETAIL_TAB` числится у app id `2f3c0ba9-4253-4da4-9aaa-6a9ee4bb4e61`; VibeCode переписал handler на свой gateway `https://vibecode.bitrix24.tech/v1/bitrix-handler`, а исходный appUrl сохранён как requested handler.
- ✅ **Этап 1 (Backend/BFF) — код и инфраструктура на новом портале закрыты**: app key, папка Диска, деплой, Puppeteer, UF-поля и `CRM_DEAL_DETAIL_TAB` готовы. Остаётся пользовательская проверка полного цикла внутри настоящего iframe Bitrix24.
- ✅ **Эндпоинт `POST /api/order-pdf` реализован** (specification.md раздел 5) — `apps/backend/src/{services/orderPdf.ts, routes/orderPdf.ts}`: HTML-шаблон бланка заказа (лейблы из общего `@garment/shared-types` — см. ниже, `escapeHtml` на весь пользовательский текст, валидация цвета regex'ом перед вставкой в inline-`style`, чтобы не открывать CSS-инъекцию через `bodyColor`/`trimColor`) рендерится в PDF через `puppeteer.launch({headless:true, args:['--no-sandbox','--disable-dev-shm-usage','--disable-gpu','--single-process']})` + `page.pdf({format:'a4'})` — та же связка аргументов, что подтверждена рабочей на живом сервере (см. выше). Смонтирован как `app.use('/api/order-pdf', identityMiddleware, express.json(), orderPdfRouter)` — **отдельный путь `/api/order-pdf`, а не `/api/order/pdf`**, специально чтобы не пересекаться префиксом с multer-роутом `/api/order` (multer и `express.json()` конфликтовать не должны, но раздельные пути проще для чтения route-таблицы). Тело запроса — JSON (не `FormData`, файл макета сюда не передаётся, только `layoutFileName` строкой для отображения в бланке).
  - **Версия Puppeteer — `^24.43.1`, НЕ последняя мажорная**: `puppeteer@25+` требует `node >=22.12.0` в `engines`, а рантайм VibeCode — `node20` (см. выше). `24.x` линейка заявляет `node >=18` — совместима. Если в будущем поднимать версию puppeteer, сначала проверить `npm view puppeteer@<version> engines`.
  - `npm install puppeteer` в этой машине НЕ докачал сам Chromium (`postinstall` заблокирован политикой `allow-scripts` в этом окружении) — для локальной проверки использовался системный `C:\Program Files\Google\Chrome\Application\chrome.exe` через `PUPPETEER_EXECUTABLE_PATH`. На боевом сервере VibeCode (Debian, root) `npm install puppeteer` докачивает Chromium сам (уже подтверждено в предыдущей сессии) — эта проблема с `allow-scripts` только локальная, не переносится на деплой.
  - Тесты: `services/orderPdf.test.ts` (5 тестов — юнит-тесты чистой функции `buildOrderHtml`: подписи всех полей, суммирование размеров, пустые/дефолтные значения, экранирование HTML в комментарии, невалидный цвет не протекает в `style`), `routes/orderPdf.test.ts` (4 теста на моках `renderOrderPdf` — 401 без заголовка, 400 при неполных данных, 200 с правильными `Content-Type`/`Content-Disposition`, 502 при падении рендера).
  - **Реальный ручной прогон** (не только моки): временный скрипт вызвал `renderOrderPdf()` напрямую с реальным Puppeteer+Chrome, результат — валидный PDF (~73 КБ), текст извлечён через `pdfjs-dist` и сверен — все поля и кириллица отрендерились корректно. Скрипт удалён после проверки (не коммитился).
- ✅ **Кнопка «Скачать файл заказа в PDF» подключена на фронтенде** — `apps/frontend/src/{features/order/exportOrderPdf.ts, components/ExportPdfButton.tsx}`, добавлена в `App.tsx` в правую колонку после `TypeMethodCard` (как в `design/maket-6.html`). JSON `POST /api/order-pdf`, скачивание ответа через `URL.createObjectURL`/`<a download>`. 4 теста (`exportOrderPdf.test.ts` — успех + автоскачивание, `layoutFileName` опускается без файла, серверная ошибка пробрасывается, сетевая ошибка). **Проверено вручную на dev-сервере полным циклом** через `preview_*`: клик по кнопке → реальный HTTP-запрос доходит до бэкенда → `401 Unauthorized` (нет заголовка `X-Vibe-Authorization` вне iframe Gateway — тот же ожидаемый паттерн, что и у «Отправить в сделку») → сообщение `"Заголовок X-Vibe-Authorization обязателен"` корректно показано пользователю через `Alert`.
- ✅ **Общие лейблы вынесены в `@garment/shared-types`** (был дубль между фронтендом и бэкендом): `PRODUCT_TYPE_LABELS`, `FABRIC_LABELS`, `CARE_LABELS`, `PRINT_METHOD_LABELS`, `PRINT_ZONE_LABELS`, `OPTION_LABELS` теперь определены один раз в `packages/shared-types/src/index.ts`, `apps/frontend/src/features/order/labels.ts` — просто реэкспорт оттуда (плюс локальные `COLOR_SWATCHES`/`SIZE_CHART`, которые бэкенду не нужны). Нужно было бэкенду для HTML-шаблона PDF-бланка, не дублировать русские подписи по двум местам.
- ⚠️ **Важный фикс окружения предпросмотра, НЕ баг кода**: `.claude/launch.json` для конфига `backend` раньше запускал `npm run dev --workspace apps/backend` (→ `tsx watch src/index.ts`). Обнаружено эмпирически в этой сессии: при запуске именно через `preview_start` (а не напрямую через Bash) этот процесс **зависал до `app.listen()`**, не открывая порт 3001 (процесс жив, CPU ~0, порт не слушается — `ECONNREFUSED` через прокси Vite), хотя ИДЕНТИЧНАЯ команда, запущенная вручную через Bash-инструмент, каждый раз стартовала нормально за секунды. Похоже на специфичный для этого окружения баг взаимодействия `tsx watch`'а (child-process/IPC между `tsx watch` и его воркер-процессом) с тем, как `preview_start` управляет stdio дочернего процесса. **Фикс**: `runtimeExecutable`/`runtimeArgs` конфига `backend` в `.claude/launch.json` изменены на прямой `npx tsx apps/backend/src/index.ts` (без `watch`, без `npm run` обёртки) — с этим порт открывается стабильно. Реальный `npm run dev`/`tsx watch` для обычной (не через preview-инструмент) разработки не тронут и продолжает работать как раньше. Если снова увидите `ECONNREFUSED`/`502 Bad Gateway` через прокси при рабочем `preview_start`-статусе "running" — первым делом проверить `Get-NetTCPConnection -State Listen` через PowerShell-инструмент (не Bash — он тоже может врать про сетевую видимость), не тратить время на код.
- ✅ **Этап 2 (Frontend) — основной конструктор реализован, проверен и ЗАДЕПЛОЕН на живой сервер.** UI из `design/maket-6.html` перенесён в реальные React-компоненты (Ant Design + Redux Toolkit), полностью проверен через `preview_*` инструменты (не только `npm test`) — переключение зон/вида, цвета, опции, размеры, таблица размеров, отправка формы (реальный 401 от бэкенда корректно отобразился пользователю). 22/22 фронтенд-тестов зелёные (10 исходных + 8 AVIF-пайплайн + 4 PDF-экспорт). Что реализовано:
  - `features/order/orderSlice.ts` — состояние формы + бизнес-правила (ткань↔уход синхронизация, доступные опции по типу изделия, зона→вид).
  - `features/order/{labels,previewZones,placementContext,submitOrder}.ts` — справочники, парсинг `placement_options`, отправка на `POST /api/order`.
  - `components/*Card.tsx` — все карточки выбора (ткань/уход, цвета, опции, размеры+модалка, тип+способ, зона печати, комментарий), на Ant Design (Radio.Group/Segmented/ColorPicker/Upload.Dragger/Modal — как рекомендовано `design/handoff.md`, не самописный JS).
  - `components/PreviewPanel.tsx` — полный SVG (только худи, как и в макете), включая живой рендер загруженного `.png`/`.jpg`/`.pdf` в зоне печати (`hooks/useFilePreviewUrl.ts`, `pdf.js`).
  - `components/LayoutUploadCard.tsx` — Drag&Drop, лимит 52 МБ с понятным сообщением при превышении.
  - ✅ **Адаптивное AVIF-сжатие объёмных растровых макетов реализовано** (specification.md раздел 4.1) — `apps/frontend/src/features/layoutCompression/`: `avifEncoder.ts` (реальный WASM-энкодер `@jsquash/avif`, принудительно однопоточный вариант через прямой импорт `codec/enc/avif_enc.js` + `?url`-импорт `.wasm`, чтобы не требовать COOP/COEP — недоступны в iframe-плейсменте Битрикс24), `avifProfileSearch.ts` (бинарный поиск максимального качества под потолок 48 МБ, при неудаче — ступенчатое снижение масштаба, чистая тестируемая логика), `rasterImage.ts` (canvas-хелперы), `pdfPackaging.ts` (упаковка в одностраничный PDF через `pdf-lib`, с клампом размера страницы под 14400pt), `compressLayout.ts` (оркестратор, итеративно снижает качество JPEG пока итоговый PDF не влезет в 52 МБ). `LayoutUploadCard.tsx` переписан под полный state-machine из макета (`rasterChoice`/`compressing`/`compressSuccess`/`compressError`/`vectorOversized` — растровые `.png`/`.jpg` оversize предлагают сжатие, векторные `.pdf`/`.ai`/`.eps` — только информационное сообщение), авто-скачивание готового PDF через blob-URL при успехе. 8 новых тестов (`avifProfileSearch.test.ts` — чистая логика поиска на mock-энкодере, `compressLayout.test.ts` — оркестрация retry-цикла на моках подмодулей), добавлен `clearMocks: true` в `vite.config.ts` (понадобился для корректной изоляции vi.mock между тестами). **Проверено вручную на живом dev-сервере** (не только `npm test`): инжектирован синтетический 55 МБ PNG (маленькая декодируемая картинка + случайный паддинг после IEND, чтобы не гонять патологически медленное AVIF-кодирование чистого шума) → модалка «Файл больше 52 МБ» → «Сжать файл» → реальное WASM-кодирование → «55,0 МБ → 0,1 МБ (AVIF)... Упаковано в PDF — 0,1 МБ» → автоскачивание → файл выбран макетом. `npx tsc -b` и `npx vite build` проходят чисто (в т.ч. `avif_enc.wasm`, 3.48 МБ, корректно попадает в `dist/assets` с хешем).
    - **Побочное наблюдение, НЕ баг новой фичи** (проверено отдельно на независимом минимальном PDF от `pdf-lib`, ведёт себя идентично): предпросмотр PDF через `pdf.js` в зоне печати (`useFilePreviewUrl.ts`, существовал до этой сессии) в текущем dev-окружении не рендерит миниатюру — падает в `null` и показывает плашку-заглушку с расширением вместо картинки. Не блокирует функциональность (файл всё равно корректно выбирается и уйдёт на бэкенд), но стоит перепроверить в реальном браузере пользователя/в iframe Битрикс24 на следующем шаге (пункт 4 ниже) — возможно, проблема с загрузкой `pdf.worker.min.mjs` специфична именно для этой headless-preview-среды.
    - Известная особенность синтетического UI-теста в этой сессии: инструмент `preview_click` (полноценная синтетическая последовательность мышиных событий) не долетал до React-обработчика кнопок модалки в этом дев-сервере — пришлось кликать через `element.click()` в `preview_eval`. Не связано с кодом фичи, скорее особенность окружения предпросмотра; иметь в виду при следующей UI-проверке.
  - ✅ **PDF-экспорт бланка заказа подключён** (кнопка в макете) — см. пункты выше про `POST /api/order-pdf` и `ExportPdfButton.tsx`.
  - Известная проблема совместимости (не критично, не мной внесена): antd v5 официально поддерживает React 16–18, проект на React 19 — консоль показывает предупреждение `[antd: compatible]`, всё работает корректно несмотря на предупреждение, стек зафиксирован — не пересматривать без запроса.
  - `.claude/launch.json` создан для `mcp__Claude_Preview` (конфиги `backend`/`frontend`, порты 3001/5173, плюс `backend-prod` — прод-режим, `node apps/backend/dist/index.js`, тоже порт 3001, для проверки собранного бэкенда со статикой фронтенда без Vite dev-сервера) — можно сразу стартовать через `preview_start`.
  - ✅ **Фронтенд задеплоен вместе с бэкендом на актуальный живой сервер VibeCode** (`apps/backend/src/app.ts`): Express раздаёт `apps/frontend/dist` через `express.static` + SPA-фолбэк (`app.get(/^(?!\/api).*/, ...)` → `index.html`). Проверено локально в прод-режиме и живьём на `https://app-5670766a17c1.vibecode.bitrix24.tech`: `/api/health` отвечает `200 {"status":"ok"}`, root `/` отдаёт React HTML через временный access-token.
  - Детали редеплоя (контракт `/deploy`, состав архива, найденные нюансы надёжности galaxy-приложений) — раздел «Живой тестовый сервер» ниже.

## Дальше — довести Этап 2 до конца

1. ✅ ~~Реальное AVIF-сжатие объёмных растровых макетов~~ — сделано в предыдущей сессии, см. статус Этапа 2 выше.
2. ✅ ~~Кнопка экспорта бланка заказа в PDF + бэкенд-эндпоинт с Puppeteer~~ — сделано в этой сессии, см. статус Этапа 1/2 выше.
3. ✅ ~~Задеплоить фронтенд и связать с уже задеплоенным бэкендом~~ — сделано в этой сессии (статика через `express.static` + SPA-фолбэк в том же Express-сервере), подтверждено живыми запросами. См. статус Этапа 2 и раздел «Живой тестовый сервер».
4. **Тест полного цикла внутри настоящего Битрикс24 iframe** (UF-поля и placement уже готовы; вне iframe проверены dev-браузер + прямые HTTP-запросы к живому серверу через access-token) — заодно перепроверить наблюдение про `pdf.js`-превью выше (пункт "Побочное наблюдение" в статусе Этапа 2) и реальный success-путь `POST /api/order-pdf` изнутри портала (пока подтверждён только `401` без заголовка гейтвея и прямой вызов `renderOrderPdf()` в обход HTTP — сам HTTP-успех `200` с PDF ещё не проверен живьём с настоящим `X-Vibe-Authorization`). **Следующий шаг.**

## Технический стек (зафиксирован, не пересматривать без явного запроса)

- Фронтенд: React 19 + TypeScript + Redux Toolkit + Ant Design 5 + Vite (antd официально поддерживает React 16–18, но с 19 всё работает, только предупреждение в консоли — см. статус Этапа 2).
- Бэкенд: Node.js + Express + TypeScript, деплой на BlackHole VibeCode `STANDALONE` сервер `bc-micro` (4 GB RAM), порт приложения внутри сервера — `3000`.
- Тесты: Vitest + React Testing Library (фронтенд), Vitest + supertest (бэкенд).
- Монорепо: npm workspaces — `apps/frontend`, `apps/backend`, `packages/shared-types`.
- Node.js на машине пользователя — v24.18.0. Прямое скачивание с nodejs.org/winget не работает из песочницы инструментов агента (исходящий HTTPS для произвольных сайтов блокируется), но `npm install` через реестр npm — работает.
- Если при `npm install` вылезет ошибка типов в `vite.config.ts` (`No overload matches this call`) — это конфликт версий vite/vitest, решение — выровнять их мажорные версии (уже решено один раз апгрейдом vitest до `^4.0.0`).

## Секреты — важно

Ключи `vibe_api_...`/`vibe_app_...` — **никогда не класть в файлы репозитория кроме `.env`** (в `.gitignore`), никогда в специфкацию/README/коммиты. Если пользователь присылает ключ в чат — использовать только для живых API-запросов в рамках сессии, не сохранять в память/файлы.

## Согласованный порядок работы (пользователь явно просил соблюдать)

1. Дизайн → полировка → код (дизайн уже пройден полностью).
2. После каждой согласованной итерации — обновлять `specification.md` и `README.txt`.
3. Коммитить с понятным сообщением перед push; пуш — по запросу или когда логично.
4. Не откатывать/не удалять предыдущие версии макетов (`design/maket-*.html`) — точки отката.
5. Каждую крупную функцию — покрывать тестами, прогонять `npm test` после правок.
6. Аудит кибербезопасности — после реализации ~70% функциональности (ещё рано).

## Известные открытые архитектурные решения (не потерять)

- Зона печати "Спина" и вид сзади/спереди в SVG-превью — реализованы только для типа "худи" (и в макете, и теперь в React-компоненте `PreviewPanel`); для остальных типов — заглушка-подпись "шаблон появится позже" (осознанное ограничение MVP, реальные шаблоны — следующая итерация Этапа 2).
- `pdf.js`-превью загруженного PDF-макета в зоне печати не рендерится в dev-окружении этого проекта (падает в заглушку с расширением) — не баг новой функциональности, наблюдалось и на независимом минимальном PDF; перепроверить в реальном браузере/iframe (пункт 4 плана выше).

## Файловая карта проекта

```
specification.md          — источник истины по требованиям/архитектуре
README.txt                — второй обязательный документ, обновляется синхронно
NEXT_SESSION.md            — этот файл
design/
  maket-1.html … maket-6.html  — история версий макета, maket-6 — ФИНАЛЬНАЯ (не удалять)
  handoff.md                    — developer handoff spec (Ant Design соответствия, токены, edge cases)
package.json, tsconfig.base.json, eslint.config.js, .prettierrc.json  — корень монорепо
.env.example               — шаблон переменных окружения (без секретов)
.claude/launch.json         — конфиги preview_start для mcp__Claude_Preview (backend:3001, frontend:5173); backend запускается напрямую через `npx tsx` (без `npm run`/`tsx watch` — см. заметку про зависание в статусе Этапа 1)
apps/frontend/src/
  features/order/           — orderSlice, labels (реэкспорт из shared-types), previewZones, placementContext, submitOrder, exportOrderPdf (+ *.test.ts)
  features/layoutCompression/ — avifEncoder, avifProfileSearch, rasterImage, pdfPackaging, compressLayout (+ *.test.ts) — реальный AVIF→PDF-пайплайн сжатия макетов
  components/                — карточки конструктора + PreviewPanel + ExportPdfButton (реализованы, ЗАДЕПЛОЕНЫ — apps/frontend/dist раздаётся тем же Express-сервером, что и API)
  hooks/useFilePreviewUrl.ts — живой предпросмотр png/jpg/pdf в зоне печати
apps/backend/src/
  vibecode/client.ts, middleware/identity.ts, services/deals.ts, services/disk.ts, routes/order.ts — реализованы и протестированы
  services/orderPdf.ts, routes/orderPdf.ts — HTML-шаблон + Puppeteer-рендер PDF-бланка заказа (POST /api/order-pdf)
  app.ts — API-роуты + express.static(apps/frontend/dist) + SPA-фолбэк на index.html (не-/api GET)
  (+ *.test.ts рядом с каждым файлом)
packages/shared-types/       — общие TS-типы, UF_CRM_* маппинг (коды подтверждены на живом портале), человекочитаемые лейблы (общие с фронтендом и бэкендом), собирается через tsc в dist/
.env                          — реальные секреты (VIBE_APP_KEY, VIBE_LAYOUT_FOLDER_ID), НЕ в git
```

## Живой тестовый сервер

- Актуальный `vibecode01`: id `e95ca529-f434-4432-bbb8-a7e0e8f85837`, appUrl `https://app-5670766a17c1.vibecode.bitrix24.tech`, kind `STANDALONE`, plan `bc-micro`, accessPolicy `OWNER_ONLY`.
- Старый `vibecode02`: id `801496ff-f700-4dd0-9bec-571adbc7efcb`, appUrl `https://app-2a2f90ace69d.vibecode.bitrix24.tech` — исторический контекст, не использовать.
- Для проверки живьём без браузера — минтить временный токен: `POST /v1/infra/servers/e95ca529-f434-4432-bbb8-a7e0e8f85837/access-tokens {"mode":"api-bearer"}` (личным `vibe_api_` ключом), потом `Authorization: Bearer <token>` на запросах к appUrl. Токен живёт 10 минут (`jwtExpiresAt`), можно перевыпустить через `POST .../access-tokens/:tokenId/refresh` не тратя лимит минта.
- Логи: `GET /v1/infra/servers/e95ca529-f434-4432-bbb8-a7e0e8f85837/logs?lines=100`.
- ✅ **Фронтенд деплоится вместе с бэкендом** — деплой-архив включает **предсобранный локально** `apps/frontend/dist` (не исходники фронтенда, сервер его не собирает). Полный актуальный контракт `POST /deploy` (см. `apps/backend/src/app.ts` для serving-логики, `README.txt`/`specification.md` раздел 8 для статуса):
  ```json
  {
    "source": { "content": "<base64 tar.gz архива>" },
    "runtime": "node20",
    "install": "npm install && npm run build --workspace packages/shared-types && npm run build --workspace apps/backend",
    "start": "node apps/backend/dist/index.js",
    "port": 3000,
    "healthPath": "/api/health",
    "env": { "PORT": "3000", "VIBE_APP_KEY": "<из .env>", "VIBE_LAYOUT_FOLDER_ID": "<из .env>" }
  }
  ```
  Содержимое архива (tar.gz): корневые `package.json`+`package-lock.json`+`tsconfig.base.json`, `packages/shared-types/{package.json,tsconfig.json,src/}`, `apps/backend/{package.json,tsconfig.json,src/}`, **`apps/frontend/dist/` целиком** (без `apps/frontend/package.json` и остального исходника фронтенда — директория без `package.json` внутри `apps/*` просто игнорируется npm-воркспейсами при `npm install`, проверено эмпирически, ошибок не даёт). `env` нужно передавать **при каждом деплое** — платформа перегенерирует `.env` на сервере с нуля, старые значения не сохраняются между деплоями, если их не передать явно.
  - **Puppeteer-зависимости на STANDALONE-сервере ставить через `/exec` до деплоя**, если они ещё не установлены: `POST /v1/infra/servers/e95ca529-f434-4432-bbb8-a7e0e8f85837/exec` с `timeout=600` и `apt-get install ...`. В `preStart` их лучше не класть: deploy-шаг имеет лимит 300 секунд, а apt может идти дольше.
  - Для проверки состояния сервиса использовать `/exec`: `systemctl is-active app`, `grep` по `/opt/app/.env` с маскированием `VIBE_APP_KEY`, `curl -s http://localhost:3000/api/health`. `/exec` не подгружает `.env` в своё окружение автоматически, поэтому проверять env надо через файл или передавать `env` в тело `/exec`.
- Для передеплоя после новых правок в коде — сначала `npm run build --workspace apps/frontend` локально (обновит `apps/frontend/dist`), затем собрать архив как описано выше и `POST /v1/infra/servers/e95ca529-f434-4432-bbb8-a7e0e8f85837/deploy` (не создавать новый сервер через `POST /v1/infra/servers` — это создаст второй, отдельный).
- Для повторной настройки нового портала после замены `.env` на актуальный `vibecode01` key: `npm run vibecode:setup-portal` идемпотентно проверяет UF-поля (`POST /v1/userfields/deals` только для недостающих) и привязывает `CRM_DEAL_DETAIL_TAB` к `VIBE_APP_URL`. `ensure-fields` можно запускать с `VIBE_PERSONAL_API_KEY`, но `bind-placement` требует актуальный `VIBE_APP_KEY`.
