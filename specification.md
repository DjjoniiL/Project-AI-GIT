# Спецификация: Конструктор заказов одежды с принтом

## Актуализация 2026-07-24

- Локально подтверждено: `npm test` проходит полностью (backend 26, frontend 22, shared-types 2 теста), `npm run build` проходит. Запускать эти команды из текущего Codex/sandbox окружения нужно вне sandbox Windows, иначе Vitest/Vite получают `spawn EPERM` на дочерних процессах.
- `packages/shared-types` больше не компилирует `src/**/*.test.ts` в `dist`; test-скрипт пакета ограничен исходниками (`vitest run src`). Это убирает риск прогонов скомпилированных копий тестов и не кладёт тестовые файлы в пакетный build.
- Перед живым продолжением Этапа 3 обнаружено, что текущий локальный `.env` содержит `VIBE_APP_KEY` старого портала `vibecode02.bitrix24.ru`. Для `vibecode01` нельзя выполнять `placements/bind`, проверку/создание UF-полей и другие живые операции, пока локальный env не заменён на актуальный ключ нового app id `2f3c0ba9-4253-4da4-9aaa-6a9ee4bb4e61` или актуальный ключ не передан одноразово в окружение команды. Секреты в документацию не записывать.
- Добавлен операционный скрипт `scripts/vibecode-ops.mjs` для Этапа 3: `vibecode:check-server`, `vibecode:ensure-fields`, `vibecode:bind-placement`, `vibecode:setup-portal`. Скрипт idempotent по UF-полям, сначала сверяет портал ключа через `/v1/me`, затем создаёт недостающие поля и привязывает `CRM_DEAL_DETAIL_TAB`. При текущем старом ключе он безопасно отказывается выполнять изменения.
- Проверка опубликованного owner-only URL без bearer-token возвращает ожидаемый `401 BH_LOGIN_REQUIRED`, что подтверждает живой BlackHole gateway. Полная проверка server health через временный access-token требует личный `VIBE_PERSONAL_API_KEY` актуального `vibecode01`.

## Актуализация контура 2026-07-23

Актуальный тестовый портал проекта: **`vibecode01.bitrix24.ru`**.

Сведения про `vibecode02.bitrix24.ru`, старое VibeCode-приложение, старый server id `801496ff-f700-4dd0-9bec-571adbc7efcb` и старый appUrl `https://app-2a2f90ace69d.vibecode.bitrix24.tech` ниже в документе являются историей предыдущего тестового контура. Их нельзя использовать как текущие production/test идентификаторы.

Проверка 2026-07-23 по новому личному VibeCode API-ключу:

- `GET https://vibecode.bitrix24.tech/v1/me` вернул `success=true`, `type=personal`, `portal=vibecode01.bitrix24.ru`, `accessMode=READWRITE`; scopes включают `crm`, `disk`, `placement`, `vibe:infra`, `vibe:storage`;
- первичные `GET /v1/infra/servers` и `GET /v1/apps` вернули пустые списки; после включения триала/подписки и ручного создания BlackHole-сервера рабочий контур поднят на `e95ca529-f434-4432-bbb8-a7e0e8f85837`;
- первичный `GET /v1/me` → `data.capabilities`: `servers.create.available=false`, `servers.deploy.available=false`, причина `MARKETPLACE_REQUIRED`; без коммерческого тарифа/активного VibeCode-триала по адресу `https://vibecode01.bitrix24.ru/settings/license/` создать BlackHole/galaxyApp-сервер на новом портале нельзя;
- после сообщения пользователя о включении 15-дневного доступа фактическое создание сервера через API прошло, но API показал неожиданный `monthlyCost="2000"` у общего galaxy-хоста; по просьбе пользователя контур удалён полностью, контрольный `GET /v1/infra/servers` вернул пустой список;
- перед следующим созданием сервера обязательно сверить биллинг/админку и не продолжать деплой, если API показывает неожиданный `monthlyCost` у galaxy-хоста;
- пользователь вручную создал корректный BlackHole-сервер `e95ca529-f434-4432-bbb8-a7e0e8f85837`: `STANDALONE`, `bc-micro`, monthlyCost `"600"`, appUrl `https://app-5670766a17c1.vibecode.bitrix24.tech`, `accessPolicy=OWNER_ONLY`;
- backend+frontend успешно задеплоены на этот сервер через VibeCode Deploy API; steps `runtime/install/env/systemd/start/healthcheck/tunnel_routing` прошли, сервис `app` запущен, `GET /api/health` через временный access-token вернул `{"status":"ok"}`, root `/` отдаёт React HTML;
- metadata карточки приложения обновлена отдельным `PATCH /v1/infra/servers/:id`: `displayName="Garment Print Constructor"`, `description="MVP order constructor for garment print deals in Bitrix24"`;
- выпущен новый OAuth/VibeCode app key через `POST /v1/apps`: app id `2f3c0ba9-4253-4da4-9aaa-6a9ee4bb4e61`, scopes `crm`, `disk`, `placement`, `user`, appUrl текущего сервера; сырой ключ `vibe_app_...` не фиксируется в документации;
- в "Общем диске" создана/переиспользована папка "Макеты конструктора", актуальный `VIBE_LAYOUT_FOLDER_ID=90`;
- сервер передеплоен с нормальным `VIBE_APP_KEY=<vibe_app_...>` и `VIBE_LAYOUT_FOLDER_ID=90`; через `POST /v1/infra/servers/:id/exec` подтверждены systemd `active`, наличие env без раскрытия секрета и `{"status":"ok"}` на локальном и внешнем healthcheck;
- если Marketplace/VibeCode-подписки нет и нужно выбирать тариф сервера вручную, целевой fallback для проекта — **`bc-micro`**: минимальный практичный тариф с 4 GB RAM; прежний бесплатный shared-контур примерно на 500 MB RAM не считать гарантированно доступным для нового портала;
- `GET /v1/userfields/deals` вернул `403 BITRIX_ACCESS_DENIED` с сообщением `REST is available only by subscription.` До решения подписки/REST-доступа на новом портале нельзя проверить или создать UF-поля сделок.

GitHub-контур 2026-07-23: remote `origin` настроен на `https://github.com/DjjoniiL/Project-AI-GIT.git`, локальная ветка `main` синхронизирована с `origin/main` на коммите `5f773e0`, `git push --dry-run origin main` проходит.

Практическое следствие: backend/frontend код остаётся актуальным, сервер, app key, папка Диска и деплой на `vibecode01` уже подняты. Остаётся проверить/создать UF-поля сделок после окончательного восстановления REST-доступа и заново привязать `CRM_DEAL_DETAIL_TAB` к новому приложению.
## Интеграция с Битрикс24 через платформу VibeCode

Версия: 2.4
Статус: Дизайн полностью завершён — финальный макет [`design/maket-6.html`](design/maket-6.html) (раздел 2.1). **Этап 1 (Backend/BFF) завершён по коду и заново поднят на новом портале** `vibecode01.bitrix24.ru`: создан новый app id `2f3c0ba9-4253-4da4-9aaa-6a9ee4bb4e61`, создана/переиспользована папка Диска `90`, backend+frontend задеплоены на BlackHole `STANDALONE`/`bc-micro` сервер `e95ca529-f434-4432-bbb8-a7e0e8f85837`, `/api/health` подтверждён живыми запросами. UF-поля и placement `CRM_DEAL_DETAIL_TAB` были полностью проверены на старом `vibecode02.bitrix24.ru`, но для нового портала их нужно подтвердить/привязать заново. **Этап 2 (Frontend) — конструктор, адаптивное AVIF-сжатие макетов, кнопка PDF-экспорта и деплой фронтенда полностью завершены**: весь UI на React (Ant Design + Redux Toolkit), реальное WASM AVIF→PDF-сжатие объёмных растровых макетов (раздел 4.1), кнопка «Скачать файл заказа в PDF» подключена к бэкенду (раздел 5), фронтенд собран и раздаётся тем же Express-сервером (`express.static` + SPA-фолбэк). Подробный статус, живые идентификаторы сервера/приложения и точка продолжения — `NEXT_SESSION.md`.

---

## 1. Цель проекта

MVP веб-конструктора для оформления заказов на печать одежды (футболки, худи, свитшоты, лонгсливы) со следующими возможностями:

- визуальный SVG-конструктор изделия с превью в реальном времени;
- загрузка макета принта (Drag&Drop);
- выбор параметров изделия и печать сводного PDF/JPEG документа;
- встраивание в карточку сделки Битрикс24, автоматическое создание/обновление сделки и прикрепление файла макета.

---

## 2. Архитектура

Проект строится на платформе **Битрикс24 VibeCode** — это меняет архитектуру относительно классического REST-приложения:

| Компонент | Решение |
|---|---|
| Frontend | React SPA (TypeScript, хуки, Redux Toolkit), сборка на **Vite** (вместо CRA — быстрее dev/HMR, проще настройка base path и dev-proxy к BFF при встраивании в iframe) |
| UI-библиотека | **Ant Design** (финальный выбор) |
| SVG-превью | React + JSX/SVG, модульные перекрашиваемые группы |
| Backend (BFF) | Node.js + Express + TypeScript, размещается на инфраструктуре BlackHole (Битрикс24), а не на своём хостинге |
| Тестирование | **Vitest + React Testing Library** (frontend), **Vitest + supertest** (backend) — единый test-runner на весь монорепозиторий |
| Монорепозиторий | **npm workspaces**: `apps/frontend`, `apps/backend`, `packages/shared-types` (общие TS-типы формы заказа и маппинга UF_CRM_*) |
| Линтинг/форматирование | ESLint + Prettier, единый корневой конфиг с overrides для frontend/backend |
| Авторизация | Ключ `vibe_app_...` (скоупы: `crm`, `disk`), без ручного OAuth 2.0 — сессия и идентификация пользователя приходят через Gateway (заголовок `X-Vibe-Authorization` → `GET /v1/me`). Детали двух типов ключей и формата ответа — ниже, живая проверка 2026-07-03 |
| Работа с CRM | `POST /v1/deals`, `PATCH /v1/deals/{id}` — создание/обновление сделки с полями `UF_CRM_*` |
| Файлы | Диск Битрикс24: `GET /v1/storages` → `folderId` → `POST /v1/files/upload` (base64 JSON, до 52 МБ) → привязка к сделке |
| Встраивание | Placement `CRM_DEAL_DETAIL_TAB`, привязка через `POST /v1/placements/bind` (только ключом `vibe_app_...`); контекст сделки приходит в query-параметре `placement_options` (JSON-строка `{"ID":"..."}`, не отдельным `deal_id`) |
| Хранилище данных | Собственная БД не используется — состояние живёт в сделке Битрикс24 + файле на Диске |
| Деплой | `POST /v1/infra/servers/:id/deploy` — приложение доступно по адресу вида `https://app-xxx.vibecode.bitrix24.tech` |

**Предварительное условие:** на портале Битрикс24 должна быть активна подписка **BitrixGPT + Маркетплейс** — без неё платформа VibeCode недоступна.

**Живой API проверен запросами** (портал `vibecode02.bitrix24.ru`) — часть страниц документации оказалась неактуальной (например, `/docs-content/recipes/disk-files.md` ошибочно утверждает, что загрузка файлов не покрыта Entity API — метод реально работает). Ключевые факты:

- **Два типа ключей**. `vibe_api_...` (личный) — заголовок `X-Api-Key`, без `Bearer`; годится для фоновых операций, но **не может** привязать placement и не участвует в iframe-авторизации. `vibe_app_...` (OAuth-приложение) — обязателен для встраивания вкладки: при открытии Gateway обменивает одноразовый `__init`-JWT на сессию и сам подставляет заголовок `X-Vibe-Authorization: Bearer vibe_session_...` на запросы браузера к бэкенду; бэкенд пробрасывает это как `Authorization` в `GET /v1/me`, чтобы получить личность пользователя.
- **`GET /v1/me`** — конверт `{success, data:{...}}` (`data.type`: `personal`/`oauth_app`, `data.scopes[]`, `data.auth.header`, `data.accessMode`, `data.expiresAt`). Ошибки — `{success:false, error:{code, message}}`: `INVALID_API_KEY`/`KEY_EXPIRED`/`KEY_INACTIVE`/`TOKEN_MISSING` (401), `IP_NOT_ALLOWED` (403), `RATE_LIMITED` (429, заголовок `Retry-After`).
- **Загрузка файла на Диск**: `POST /v1/files/upload`, JSON-тело `{folderId|storageId, filename, content}` (`content` — base64, **не** multipart), scope `disk`. Лимит тела запроса — 70 МБ → максимум исходного файла **52 МБ** (расчёт — раздел 4). Потоковой/chunked-загрузки в методе нет — файл кодируется в base64 целиком перед вызовом. Scope `vibe:storage` — отдельное файловое хранилище платформы, не связано с Диском Битрикс24 и не годится для вложений в сделку.
- **Puppeteer/headless Chromium**: на актуальном `STANDALONE`/`bc-micro` сервере системные библиотеки установлены через `POST /v1/infra/servers/:id/exec` (`apt-get install ...`, exitCode 0). Для свежего сервера сначала выполнить `/exec` с `timeout=600`, затем деплой; fallback `pdf-lib`/`@react-pdf/renderer` держать только как аварийный запас.
- **`POST /v1/infra/servers/:id/deploy`**: для актуального BlackHole `STANDALONE` сервера Dockerfile не нужен — приложение задаётся через `runtime`/`install`/`start`/`port`. Обязательно: `runtime` (`node20`), `start` (одна команда), `source.content` как base64 `tar.gz`. `install` — одна строка без переноса. Постоянные данные — только `/data`. Для обслуживания сервера использовать `POST /v1/infra/servers/:id/exec` в JSON-режиме; `.env` из деплоя не подгружается в `/exec` автоматически, поэтому env проверять через `/opt/app/.env` с маскированием секретов.
- **`POST /v1/placements/bind`**: требует `vibe_app_` ключ (личным недоступно). `CRM_DEAL_DETAIL_TAB` подтверждён живым `GET /v1/placements/available` (из 55 кодов). Контекст сделки приходит **не** как `deal_id` в query, а как `placement_options` — JSON-строка `{"ID":"42"}` после handler-редиректа (`POST /v1/bitrix-handler` → `<appUrl>/?placement=...&member_id=...&placement_options=<JSON>&__init=<JWT>`); парсить через `URLSearchParams` + `JSON.parse`, **не** через `BX24.placement.info()` (SDK не видит контекст через редирект). Для self-hosted `appUrl` — код обменивается вручную через `POST /v1/oauth/token`.
- **UF-поля создаются программно**: `POST /v1/userfields/deals {userTypeId, fieldName, label}`, каталог типов — `GET /v1/userfields/deals/types` (включает `file` — использован для поля «Файл макета» напрямую на сделке, `UF_CRM_DESIGN_FILE_ID`). Все 11 полей из раздела 6 созданы на портале (Этап 1) — коды предсказуемы: `UF_CRM_` + переданный `fieldName`, без случайного суффикса.

**Секреты**: ключи `vibe_app_...` / `vibe_api_...` — только в `.env` (не коммитится) или в секретах платформы VibeCode, никогда в спецификации/README/коде/истории коммитов.

## 2.1 Порядок реализации (итерации)

Разработка велась согласованными итерациями, дизайн — до написания production-кода.

1. **Итерации 1–2 — дизайн (завершены).** Черновой макет → `design-critique` → `accessibility-review` (WCAG 2.1 AA) → `ux-copy` → `design-system` → `design-handoff`. Итоговый developer handoff-спек — [`design/handoff.md`](design/handoff.md): маппинг токенов CDS → Ant Design theme, компоненты (Radio.Group/Segmented/ColorPicker/Upload.Dragger/Modal вместо ручного JS). Версии — [`design/maket-1.html`](design/maket-1.html) … [`design/maket-6.html`](design/maket-6.html), у каждой полный список отличий от предыдущей — в шапке-комментарии файла; не удалять, это точки отката.
2. **Финальный макет — [`design/maket-6.html`](design/maket-6.html), полностью согласован пользователем.** Поверх Итерации 2 добавлены: округление силуэта, вид спереди/сзади, зона печати «Спина», лимит файла 52 МБ (раздел 4), обработка объёмных растровых макетов (раздел 4.1).
3. **Итерация 3+ — каркас проекта.** Реализация по Этапам 0–5 (раздел 8), начата после согласования финального дизайна.

После каждой согласованной итерации — обновление `specification.md` и `README.txt`.

---

## 3. Параметры конструктора (объём — строго по исходному README)

### 3.1 Тип изделия
Футболка, худи, свитшот, лонгслив.

### 3.2 Ткань
Футер 2х или 3х нитка, с начёсом или без.

### 3.3 Цвет — 2 зоны
Принято решение об усреднённом варианте детализации:

- **Основной цвет** — цвет тела/силуэта изделия;
- **Цвет отделки** — цвет элементов отделки (капюшон, манжеты, низ, карман, шнурки — то есть те детали, которые в README перечислены в "дополнительных опциях").

SVG-превью строится из **модульных перекрашиваемых групп** (минимум: тело/силуэт как одна группа с основным цветом, элементы отделки как вторая группа с цветом отделки), а не единой заливки на всё изделие.

### 3.4 Размер и количество
Таблица S–7XL с полями ввода количества по каждому размеру.

### 3.5 Дополнительные опции
Капюшон, карман, молния, отделка и др. (реализуются как чекбоксы/переключатели, влияющие на видимость соответствующих элементов SVG).

### 3.6 Комментарий
Свободный текст.

### 3.7 Уход за материалом
Одиночный выбор из двух вариантов: **Долговечный** (стирка 60°) / **Бережный уход** (стирка 40°). Влияет только на данные заказа (UF-поле), на SVG-превью не отображается.

### 3.8 Способ нанесения принта
Одиночный выбор: **ДТФ**, **Сублимация**, **Вышивка**.

### 3.9 Зона размещения принта
Одиночный выбор из четырёх зон на изделии: **Грудь слева**, **Вся грудь**, **Рукав**, **Спина** (последняя — добавлена по итогам полировки макета №5/6, вопреки исходному плану "вне MVP", см. историю решений в шапке `design/maket-5.html`; реализована и в макете, и в реальном коде `apps/frontend`). Выбор зоны «Спина» автоматически переключает превью на вид сзади (отдельный вид изделия сзади реализован для типа «худи»). На SVG-превью выбранная зона отмечена пунктирной рамкой; после загрузки растрового макета (`.png`/`.jpg`) файл отображается внутри рамки, при смене зоны — переносится без повторной загрузки. Для PDF — то же самое (живой рендер страницы через `pdf.js`, раздел 4.1). Для векторных `.ai`/`.eps` вместо превью — плашка с расширением файла (см. раздел 4).

> **Решение по расширению функциональности:** поля "Тираж", "Сигнальный образец", "Бирки", "Силуэт", замеченные в референсном проекте (`constructor.all4promo.ru`), в MVP **не включаются** — остаёмся в границах исходного README. Параметры 3.7–3.9 (уход за материалом, способ нанесения принта, зона размещения принта), напротив, добавлены по итогам согласования Итерации 1 — 2026-07-01 — и входят в объём MVP.

---

## 4. Загрузка файлов макета

- Форматы: `.png`, `.jpg`, `.pdf`, `.ai`, `.eps`.
- **Максимальный размер: 52 МБ** (скорректировано 2026-07-03 по факту разведки живого API, было 115 МБ в исходном ТЗ — см. раздел 2). Причина: `POST /v1/files/upload` на Диск Битрикс24 принимает файл только как base64 в JSON-теле, лимит тела запроса на платформе — 70 МБ; base64 раздувает размер в 4/3 раза (+33%), поэтому исходный файл не должен превышать `70 × 0.75 = 52.5` МБ — 52 МБ взято с небольшим запасом на служебные JSON-поля. Проверка исходного размера — на фронтенде и на бэкенде, до base64-кодирования, чтобы не тратить время на заведомо неудачную загрузку (`422 BITRIX_ERROR` при превышении на стороне Битрикс24).
- Загрузка на бэкенд от фронтенда — потоком (streaming, `multipart/form-data`), не в память целиком; но перед финальной отправкой на Диск Битрикс24 файл неизбежно буферизуется и кодируется в base64 целиком — метод API не поддерживает потоковую/chunked-загрузку (см. раздел 2). При 52 МБ максимума это приемлемо.
- Drag&Drop-зона.
- Для `.ai`/`.eps` — живой предпросмотр в браузере невозможен (векторные форматы Illustrator/EPS); показываем иконку-заглушку с именем файла. Сам файл при этом полноценно загружается на Диск Битрикс24 и прикрепляется к сделке.
- Валидация MIME по содержимому файла, а не только по расширению.

### 4.1 Обработка объёмных растровых файлов (AVIF → PDF)

Растровые `.png`/`.jpg`/`.jpeg` больше 52 МБ (пример — 8K UHD, 126–150 МБ): вместо отказа — модалка **«Сжать файл»** / **«Выбрать другой»**. Проверка размера — на клиенте, сразу после выбора/drop, до отправки на бэкенд.

Векторные `.pdf`/`.ai`/`.eps` больше лимита — только информационное сообщение, сжатие недоступно. **Рассмотрено и сознательно отложено**: PDF можно было бы сжать через растеризацию `pdf.js` на клиенте (теряет векторную точность текста/линий — нежелательно для вышивки/крупноформатной печати) или настоящим PDF-сжатием с сохранением вектора (требует бэкенда — Ghostscript-класс инструментов, клиентского аналога нет). Не переоткрывать без запроса пользователя.

Пайплайн «Сжать файл»:
1. **Адаптивное AVIF-кодирование на клиенте** (WebAssembly, кандидат `@jsquash/avif`; финальный выбор — Этап 2) — качество (и разрешение как вторая ступень) подбирается итеративным поиском под конкретный файл, цель — максимум качества в пределах потолка **48 МБ**, не фиксированный коэффициент.
2. **Конвертация в PDF** (одностраничный документ со встроенным изображением) — стандартные PDF-библиотеки не embed-ят AVIF-поток напрямую, растровые данные переупаковываются в JPEG/PNG внутри PDF; потолок **52 МБ** (запас ~4 МБ на переупаковку).

Клиентская обработка (не серверная) — чтобы файл до 150 МБ не долетал до бэкенда, который вдобавок должен тянуть Puppeteer и base64-загрузку в Диск.

Итоговый PDF — окончательный производственный файл: идёт в SVG-превью, автоматически скачивается пользователю (`<a download>`/blob-URL) и грузится на Диск/в сделку. PDF как финальный формат (не AVIF) снимает риск несовместимости с печатным оборудованием/RIP-софтом.

**Живое превью PDF на изделии**: реализовано через `pdf.js` — страница рендерится в канвас (`getDocument`→`getPage(1)`→`render`→`toDataURL()`), вставляется в SVG `<image>` тем же способом, что `.png`/`.jpg`. Работает и для PDF, загруженного напрямую (≤52 МБ), и для итогового PDF после сжатия — превью и файл на Диске технически совпадают. Для `.ai`/`.eps` живого превью нет — клиентского способа их отрендерить не существует, остаётся плашка с расширением.

Edge case: если итоговый PDF всё же вышел бы за 52 МБ (маловероятно) — на Этапе 2 повторить с более агрессивным качеством AVIF; при повторной неудаче — тот же сценарий, что для векторных форматов.

Макет модалки — [`design/maket-6.html`](design/maket-6.html), финализирован, включает демо-кнопки для прогона обоих сценариев без реального крупного файла.

---

## 5. Экспорт результата

По результатам заполнения формы — скачивание **PDF или JPEG** со сводной таблицей выбранных параметров (аналог производственной спецификации/карты раскроя). Рендер — на бэкенде через HTML-шаблон + Puppeteer (или аналог), без завязки на скриншот SVG.

---

## 6. Пользовательские поля сделки (UF_CRM_*)

Список фиксируется на Этапе 0, до начала разработки бэкенда, чтобы избежать переделки маппинга. Минимальный набор, исходя из п. 3:

- тип изделия
- ткань
- уход за материалом (долговечный 60° / бережный 40°)
- способ нанесения принта (ДТФ / сублимация / вышивка)
- основной цвет
- цвет отделки
- размеры и количество (JSON-строка)
- доп. опции (капюшон/карман/молния/отделка)
- зона размещения принта (грудь слева / вся грудь / рукав)
- комментарий
- ссылка/ID файла макета на Диске

---

## 7. Встраивание в Битрикс24

1. Пользователь открывает вкладку в карточке сделки (placement `CRM_DEAL_DETAIL_TAB`, подтверждён живым `GET /v1/placements/available`). Требует ключ `vibe_app_...`, привязку делает `POST /v1/placements/bind` (личным ключом `vibe_api_...` недоступно — см. раздел 2).
2. Gateway обменивает одноразовый `__init`-JWT на сессию и редиректит iframe на `<appUrl>/?placement=CRM_DEAL_DETAIL_TAB&member_id=...&placement_options=<JSON>&__init=...`. Frontend **не** получает `deal_id` напрямую — вместо этого парсит query-параметр `placement_options` (JSON-строка вида `{"ID":"42"}`, `URLSearchParams.get('placement_options')` + `JSON.parse`) и достаёт `ID` сделки оттуда. Через `BX24.placement.info()` контекст недоступен (SDK не видит его через handler-редирект).
3. Пользователь заполняет форму, загружает макет.
4. Frontend отправляет данные на BFF (`multipart/form-data`).
5. BFF проверяет идентичность пользователя: Gateway сам подставляет заголовок `X-Vibe-Authorization: Bearer vibe_session_...` на каждый запрос браузера к бэкенду — BFF пробрасывает его как `Authorization` в `GET /v1/me`, валидирует данные:
   - если `dealId` (из `placement_options`) передан — `PATCH /v1/deals/{id}`;
   - если отсутствует (открытие вне карточки сделки, если такой сценарий разрешён) — `POST /v1/deals`;
   - загружает файл на Диск (`POST /v1/files/upload`, до 52 МБ — раздел 4) и привязывает к сделке.
6. Возвращает статус операции на frontend.

---

## 8. План работ (обновлён под платформу VibeCode)

Этапы 0–5 ниже относятся к Итерации 3+ (раздел 2.1) — кодирование начинается только после согласования дизайна в Итерациях 1–2.

### Этап 0 — Подготовка (сокращён)
- ✅ Инициализация монорепозитория: npm workspaces (`apps/frontend`, `apps/backend`, `packages/shared-types`), корневой ESLint (flat config) + Prettier, `tsconfig.base.json`. `npm install`, `npm run build`, `npm test`, `npm run lint` проходят чисто с корня.
  - `apps/frontend` — Vite + React 19 + TypeScript, Redux Toolkit и Ant Design подключены (плейсхолдер-экран), Vitest + React Testing Library.
  - `apps/backend` — Express + TypeScript, точка входа + `GET /api/health`, Vitest + supertest.
  - `packages/shared-types` — общие TS-типы формы заказа, потребляются напрямую из исходников (без отдельной сборки).
  - Стек тестирования обновлён по факту установки: **Vitest 4.x** (а не 2.x) — версия 2.x несовместима с Vite 8 (конфликт версий vite у vitest и `@vitejs/plugin-react`), потребовался апгрейд для единой версии vite в дереве зависимостей.
  - Node.js установлен вручную пользователем (v24.18.0, LTS на момент установки) — `winget`/скачивание файлов недоступны из песочницы инструментов агента (исходящий HTTPS для скачивания блокируется, но реестр npm — доступен).
- ✅ Фиксация списка UF-полей (раздел 6) в `packages/shared-types/src/index.ts` (`UF_FIELD_MAP`) — **коды подтверждены живыми полями на портале** (см. Этап 1 ниже), править больше не нужно.
- ✅ Определена JSON-структура размеров/количества — `SizeQuantities` (`Partial<Record<Size, number>>`) в `packages/shared-types`.
- ✅ Разведывательный проход по живой документации VibeCode и тестовому ключу — выполнен 2026-07-03, результаты и найденные расхождения документации с реальным API — раздел 2.
- ✅ Создание ключа `vibe_app_...` — выполнено программно через `POST /v1/apps` (не вручную через интерфейс портала, как предполагалось изначально), см. Этап 1.

### Этап 1 — Backend / BFF — ✅ ЗАВЕРШЁН, проверен на живом портале `vibecode02.bitrix24.ru`
- ✅ `POST /api/order` с валидацией (`apps/backend/src/routes/order.ts`, multer, лимит 52 МБ).
- ✅ Identity-middleware на основе `X-Vibe-Authorization` + `GET /v1/me` (`middleware/identity.ts`).
- ✅ `createOrUpdateDeal(dealData, dealId?)` (`services/deals.ts`).
- ✅ `uploadFileToDisk(file, dealId)` (`services/disk.ts`).
- ✅ Обработка ошибок — единый конверт `{success,error:{code,message}}`, статусы прокинуты от VibeCode API. Логирование — стандартный вывод (расширенное логирование — по необходимости).
- ✅ 11 UF-полей были созданы и проверены на старом портале `vibecode02` (`POST /v1/userfields/deals`), коды совпали с `UF_FIELD_MAP` без правок; на новом `vibecode01` их нужно подтвердить/создать после восстановления REST-доступа.
- ✅ Актуальный ключ `vibe_app_...` для `vibecode01` создан (scopes `crm`+`disk`+`placement`+`user`), папка на Диске под макеты создана/переиспользована (`VIBE_LAYOUT_FOLDER_ID=90`).
- ✅ Бэкенд и фронтенд задеплоены на инфраструктуру VibeCode (`STANDALONE`/BlackHole `bc-micro`, `POST /v1/infra/servers/:id/deploy` с `source.content`), `GET /api/health` подтверждён живым запросом. Реальный `folderId`/ключ/id сервера — `NEXT_SESSION.md` (секреты не хранятся в спецификации).
- ✅ **Puppeteer/headless Chromium подготовлен на актуальном сервере** после доустановки системных библиотек через `/exec` (`apt-get install libglib2.0-0 libnss3 libatk1.0-0 ...` — Ubuntu 24.04, root-доступ). Fallback на `pdf-lib` не понадобился.
- ✅ **Реализован эндпоинт `POST /api/order-pdf`** (раздел 5) — `apps/backend/src/{services/orderPdf.ts, routes/orderPdf.ts}`: HTML-шаблон бланка заказа (человекочитаемые подписи из общего `@garment/shared-types`, экранирование пользовательского текста, валидация цвета перед вставкой в inline-стиль) рендерится в PDF через `puppeteer` (`^24.43.1` — не последняя мажорная версия, т.к. `puppeteer@25+` требует Node ≥22.12, а рантайм VibeCode — `node20`; `24.x` заявляет `node >=18`). Подключён под тем же `identityMiddleware`, что и `/api/order` (mount — `/api/order-pdf`, не `/api/order/pdf`, чтобы не пересекаться с multer-роутом `orderRouter` по префиксу). Проверено: юнит-тесты HTML-шаблона (лейблы, экранирование, edge cases пустых полей), тесты роута на моках `renderOrderPdf` (401/400/200+заголовки/502), и **реальный ручной прогон** — Puppeteer + локальный Chrome отрендерили настоящий PDF, текст (включая кириллицу) извлечён и сверен через `pdfjs-dist`.

### Этап 2 — Frontend / SVG-конструктор — ✅ полностью завершён и задеплоен
- ✅ Компоненты выбора (тип, ткань, уход за материалом, способ нанесения принта, 2 цветовые зоны, размеры/количество, доп. опции, зона размещения принта, комментарий) — `apps/frontend/src/components`, на Ant Design (Radio.Group/Segmented/ColorPicker/Upload.Dragger/Modal — по рекомендации `design/handoff.md`).
- ✅ Загрузчик файлов с Drag&Drop; лимит 52 МБ с понятным сообщением при превышении.
- ✅ Модульное SVG-превью (группы: тело/силуэт, элементы отделки) с раздельной перекраской, видом спереди/сзади и наложением загруженного макета (`.png`/`.jpg`/`.pdf` — живой рендер через `pdf.js`) в выбранной зоне печати.
- ✅ Сбор данных и отправка на backend (`POST /api/order`, `FormData`) — проверено вручную в браузере (реальная ошибка `401` от бэкенда корректно отображается пользователю в dev-режиме, где нет заголовка Gateway).
- ✅ **Адаптивное AVIF-сжатие объёмных растровых макетов реализовано** (раздел 4.1, `apps/frontend/src/features/layoutCompression`): реальное WASM-кодирование (`@jsquash/avif`, принудительно однопоточный вариант — без COOP/COEP), бинарный поиск максимального качества (`avifProfileSearch.ts`) с падением на пониженное разрешение при необходимости, потолок 48 МБ; переупаковка в JPEG внутри одностраничного PDF (`pdf-lib`, `pdfPackaging.ts`) с итеративным снижением качества JPEG под потолок 52 МБ (`compressLayout.ts`). Проверено вручную на dev-сервере полным циклом (модалка выбора → сжатие → автоскачивание PDF → выбор макетом) и модульными тестами на чистой логике поиска и оркестрации.
- ✅ **Кнопка экспорта бланка заказа в PDF реализована** (раздел 5): `apps/frontend/src/{features/order/exportOrderPdf.ts, components/ExportPdfButton.tsx}` — JSON `POST /api/order-pdf` (без файла макета, только имя для отображения в бланке), скачивание готового PDF через blob-URL. Место в разметке — правая колонка после блока «Тип изделия/Способ нанесения», как в `design/maket-6.html`. Проверено вручную на dev-сервере: клик → реальный сетевой запрос → `401` от бэкенда (нет заголовка Gateway вне iframe, тот же ожидаемый паттерн, что и у «Отправить в сделку») корректно отображается пользователю через `Alert`.
- ✅ **Фронтенд собран и задеплоен вместе с бэкендом** (раздел 8, Этап 5): `apps/backend/src/app.ts` раздаёт `apps/frontend/dist` через `express.static` + SPA-фолбэк (любой не-`/api` GET → `index.html`, чтобы плейсмент, открывающий базовый URL приложения с query-параметрами, а не отдельным путём, всегда получал React-приложение). Фронтенд собирается **локально** (`npm run build --workspace apps/frontend`) и упаковывается готовым в деплой-архив — сервер его не компилирует. Подтверждено прямыми HTTP-запросами к живому серверу через временный access-token: `/` и произвольный SPA-путь отдают `index.html` (200, `text/html`), `/api/health` — 200, JS/WASM-ассеты грузятся корректно, `/api/order-pdf` без заголовка гейтвея по-прежнему верно отвечает `401` (статик-сервинг не задел авторизацию API).
- ❌ **Не реализовано**: заглушка вне MVP по-прежнему актуальна только для превью типов кроме худи ("шаблон появится позже"). Замечено (не блокер, вне рамок этой задачи): предпросмотр PDF через `pdf.js` в зоне печати (раздел 4.1) в текущем dev-окружении не рендерит миниатюру даже для валидных небольших PDF — falls back на плашку с расширением; сам факт того, что PDF выбран макетом и уйдёт на бэкенд, не затронут.

### Этап 3 — Встраивание в Битрикс24 — 🔶 частично выполнено
- ✅ Механизм получения `dealId` — не простой query-параметр, а `placement_options` (раздел 7), уже реализован на фронтенде (`features/order/placementContext.ts`) и проверен на старом портале.
- ❌ Привязку placement `CRM_DEAL_DETAIL_TAB` через `POST /v1/placements/bind` нужно выполнить заново для нового app id `2f3c0ba9-4253-4da4-9aaa-6a9ee4bb4e61`.
- ❌ Проверка полного цикла внутри настоящего Битрикс24 iframe на новом `vibecode01` ещё не выполнялась. **Следующий шаг после bind/UF-полей.**

### Этап 4 — Тестирование
- Юнит/интеграционные тесты на каждую крупную функцию (Vitest + React Testing Library на фронтенде, Vitest + supertest на бэкенде), автопрогон (`npm test` в корне монорепозитория) после каждой правки.
- Проверка сценариев: создание сделки, обновление, загрузка разных форматов файлов, превышение лимита 52 МБ, работа внутри iframe.

### Этап 5 — Деплой и документация — 🔶 бэкенд+фронтенд задеплоены единым сервером, финальная документация — по завершении проекта
- ✅ Бэкенд и собранный фронтенд деплоятся вместе через `POST /v1/infra/servers/:id/deploy` на актуальный `STANDALONE`/BlackHole сервер. Источник — `source.content`, base64 tar.gz архива; `runtime`/`start` обязательны. Полный актуальный контракт запроса, состав архива и нюансы `/exec` — `NEXT_SESSION.md`, раздел «Живой тестовый сервер» (секреты и точные значения туда, не в спецификацию).
- ✅ Обновление спецификации и README по итогам реализации — выполняется после каждой значимой итерации (в т.ч. эта).
- ❌ Финальная сквозная документация проекта (после завершения Этапа 3/4) — ещё рано.

### Аудит безопасности
Проводится после согласования и реализации ~70% функциональности. Зоны внимания:
- валидация файлов (MIME по содержимому, лимиты, защита от path traversal в имени файла);
- корректность CSP (`frame-ancestors`) для работы во вложенном iframe;
- отсутствие утечек в логах (файлы, персональные данные из сделки).

---

## 9. Требования к процессу разработки (из исходного README, без изменений)

- После каждой итерации — обновление спецификации и README.
- Каждая крупная функция покрывается тестами.
- Автоматический прогон тестов после внесения правок.
- В процессе разработки — анализ и предложения по оптимизации стабильности/быстродействия.
- Аудит кибербезопасности после реализации ~70% проекта.
